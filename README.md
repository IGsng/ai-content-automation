# 🤖 AI Content Automation System

**Полностью автоматизированная система для генерации вирусного видеоконтента с использованием локальных AI-моделей**

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](Dockerfile)

## 🎯 Что делает система

Автоматически создает короткие видео (30-60 сек) для TikTok/Instagram/YouTube:

1. **🔍 Поиск интересных фактов** — локальная LLM находит необычные темы
2. **✍️ Создание сценария** — генерирует цепляющий текст для видео
3. **🎤 Озвучка** — text-to-speech с естественным голосом
4. **🎥 Генерация видео** — AI создает визуальный ряд
5. **✂️ Монтаж** — склейка аудио, видео, субтитры
6. **📤 Публикация** — автопостинг в соцсети

## 🚀 Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
git clone https://github.com/IGsng/ai-content-automation.git
cd ai-content-automation

# Настройка переменных окружения
cp .env.example .env
nano .env  # Заполни API ключи

# Запуск всего стека
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### Вариант 2: Локальная установка

```bash
# Требования: Python 3.10+, FFmpeg, CUDA 11.8+ (для GPU)

# Установка зависимостей
pip install -r requirements.txt

# Установка Ollama (LLM)
curl -fsSL https://ollama.com/install.sh | sh
ollama pull qwen2.5:32b

# Запуск системы
python main.py
```

## 📋 Требования

### Минимальные (CPU режим)
- CPU: Intel i7 / AMD Ryzen 7
- RAM: 16 GB
- Диск: 50 GB свободного места
- Генерация: ~5-10 мин на видео

### Рекомендуемые (GPU режим)
- CPU: Intel i9 / AMD Ryzen 9
- RAM: 32 GB
- GPU: NVIDIA RTX 3060 (12GB VRAM) или выше
- Диск: 100 GB SSD
- Генерация: ~2-3 мин на видео

### Оптимальные
- GPU: RTX 4090 (24GB) / A6000 (48GB)
- RAM: 64 GB
- Генерация: ~30-60 сек на видео

## 🏗️ Архитектура проекта

```
ai-content-automation/
├── src/
│   ├── fact_generator/      # Генерация интересных фактов
│   ├── script_writer/       # Создание сценариев
│   ├── voice_synthesis/     # Text-to-Speech озвучка
│   ├── video_generator/     # AI генерация видео
│   ├── video_editor/        # Монтаж и постобработка
│   ├── publisher/           # Публикация в соцсети
│   └── utils/               # Вспомогательные утилиты
├── config/
│   ├── prompts.yaml         # Промпты для LLM
│   ├── settings.yaml        # Настройки системы
│   └── platforms.yaml       # Конфиги соцсетей
├── workflows/
│   └── n8n/                 # n8n автоматизация
├── models/                  # Директория для AI моделей
├── output/                  # Готовые видео
├── logs/                    # Логи работы
├── tests/                   # Юнит-тесты
├── docker-compose.yml       # Docker окружение
├── Dockerfile              # Контейнер приложения
├── requirements.txt         # Python зависимости
└── main.py                  # Точка входа
```

## ⚙️ Конфигурация

### .env файл

```env
# LLM Settings
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:32b

# Video Generation
VIDEO_API_PROVIDER=replicate  # replicate / local / fal
REPLICATE_API_TOKEN=your_token_here

# Text-to-Speech
TTS_PROVIDER=silero  # silero / elevenlabs / yandex
ELEVENLABS_API_KEY=your_key_here

# Social Media APIs
BLOTATO_API_KEY=your_key_here
UPLOAD_POST_API_KEY=your_key_here

# Publishing Settings
PUBLISH_TO_TIKTOK=true
PUBLISH_TO_INSTAGRAM=true
PUBLISH_TO_YOUTUBE=true

# Scheduling
VIDEOS_PER_DAY=3
GENERATION_HOURS=09:00,15:00,21:00
```

## 🎬 Использование

### Базовый запуск

```python
from src.pipeline import ContentPipeline

# Инициализация
pipeline = ContentPipeline()

# Генерация одного видео
video = pipeline.generate_video(
    topic="космос",
    duration=45,
    style="энергичный"
)

# Публикация
pipeline.publish(video, platforms=["tiktok", "instagram"])
```

### CLI интерфейс

```bash
# Генерация видео по теме
python cli.py generate --topic "интересные факты про океан" --duration 60

# Публикация существующего видео
python cli.py publish --video output/video_123.mp4 --platforms tiktok,instagram

# Запуск по расписанию
python cli.py schedule --videos-per-day 3

# Статистика
python cli.py stats --last-days 7
```

## 🔧 Продвинутые настройки

### Кастомные промпты

Отредактируй `config/prompts.yaml`:

```yaml
fact_generation:
  system: |
    Ты исследователь необычных фактов.
    Критерии:
    - Проверенная информация
    - Удивительность
    - Визуализируемость
    
  user_template: |
    Найди интересный факт на тему: {topic}
    Целевая аудитория: {audience}
    Длительность видео: {duration} секунд

script_writing:
  system: |
    Ты сценарист вирусных коротких видео.
    Структура:
    1. Хук (3 сек) - зацепить внимание
    2. Основная часть - раскрыть тему
    3. CTA - призыв к действию
```

### Локальная генерация видео

Установи ComfyUI для локальной генерации:

```bash
# В директории models/
git clone https://github.com/comfyanonymous/ComfyUI
cd ComfyUI
pip install -r requirements.txt

# Скачай модель Wan 2.2
wget https://huggingface.co/Wan-AI/Wan2.2-T2V-A14B/resolve/main/model.safetensors \
  -P models/checkpoints/

# Запуск ComfyUI API
python main.py --listen 0.0.0.0 --port 8188
```

В `.env` измени:
```env
VIDEO_API_PROVIDER=local
COMFYUI_HOST=http://localhost:8188
```

## 📊 Мониторинг и аналитика

### Веб-интерфейс

Запусти дашборд:

```bash
python dashboard.py
# Открой http://localhost:8080
```

Доступно:
- 📈 Статистика генерации
- 👁️ Просмотры и вовлеченность
- 💰 Расходы на API
- 🎯 Топ-темы по эффективности

### Логирование

Все логи в `logs/`:
- `generation.log` — процесс создания
- `publishing.log` — постинг в соцсети
- `errors.log` — ошибки системы

## 🧪 Тестирование

```bash
# Все тесты
pytest tests/

# Конкретный модуль
pytest tests/test_fact_generator.py -v

# С покрытием
pytest --cov=src tests/
```

## 🐳 Docker развертывание

### Полный стек

```yaml
# docker-compose.yml включает:
- app (основное приложение)
- ollama (LLM сервер)
- comfyui (опционально, видео генерация)
- redis (кэш и очереди)
- postgresql (база данных)
- n8n (автоматизация workflow)
```

### Масштабирование

```bash
# Несколько воркеров для параллельной генерации
docker-compose up --scale app=3
```

## 🔐 Безопасность

- Все API ключи в `.env` (не коммить!)
- `.gitignore` исключает секреты
- Rate limiting для API запросов
- Валидация контента перед публикацией

## 🤝 Вклад в проект

1. Fork репозитория
2. Создай feature ветку (`git checkout -b feature/amazing-feature`)
3. Коммит изменений (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Открой Pull Request

## 📝 Roadmap

- [x] Базовая генерация контента
- [x] Интеграция с TikTok/Instagram/YouTube
- [x] Docker контейнеризация
- [ ] Веб-интерфейс для управления
- [ ] A/B тестирование заголовков
- [ ] Автоматическая оптимизация под алгоритмы
- [ ] Мультиязычность (EN, ES, DE)
- [ ] Telegram бот для контроля
- [ ] ML модель для предсказания вирусности

## ⚠️ Disclaimer

- Проверяй факты перед публикацией
- Соблюдай правила платформ
- Не используй для спама
- Уважай авторские права

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 💬 Поддержка

- Issues: [GitHub Issues](https://github.com/IGsng/ai-content-automation/issues)
- Telegram: @your_channel
- Email: your@email.com

## 🙏 Благодарности

- [Ollama](https://ollama.com/) за простой интерфейс к LLM
- [ComfyUI](https://github.com/comfyanonymous/ComfyUI) за видео генерацию
- [Silero](https://github.com/snakers4/silero-models) за качественный TTS
- [n8n](https://n8n.io/) за автоматизацию workflow

---

⭐ **Если проект полезен — поставь звезду!**